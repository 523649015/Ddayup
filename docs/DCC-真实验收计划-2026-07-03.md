# DCC 真实验收计划 - 2026-07-03

## 本次已完成的真实进展

### 1. Unreal 启动隔离链路先做了稳定性补强

- 已修改 [app/server/dcc/adapters/unreal-plugin-adapter.mjs](/abs/path/F:/Work/HMDAODAO/app/server/dcc/adapters/unreal-plugin-adapter.mjs:416)。
- 变更点：
  - 给 `zen.exe service status` 加了 5 秒超时。
  - 给 `zen.exe up` 加了 15 秒超时。
  - 在 `ensureZenServerReady()` 前后补了更明确的日志。
  - 普通独立启动项目的 `waitForDirectBridge()` 也会持续刷新启动日志快照，不再只盯 safe sidecar。
  - `Action.uproject` 这类“已在运行中的独立 Unreal 项目”触发 `custom-plugin-direct` connect 时，不再固定只等 2 分钟，而是会按真实启动阶段动态放宽到长等待窗口。
  - `Turnkey SDK detection complete` 这个阶段已改成自适应 stall 阈值，避免把历史上“Turnkey 后长静默但最终成功”的真实启动误判成 stalled。
  - 启动阶段识别从固定少量关键字改成按日志中“最后出现的阶段”选取，`Slate` / `Shader warmup` / `Turnkey` 等真实进度现在能在状态面板里正确反映。
  - 新拉起的 Unreal 进程会带上 `startedAtMs` 参与启动日志判定，避免旧 `Action.log` 污染新的 `Action-hmdao-launch.log` 观测。
  - 非 sidecar 的目标 Unreal 项目在真正拉起前，会先标准化本机 `Saved/Config/WindowsEditor/EditorPerProjectUserSettings.ini`，关闭恢复上次关卡/标签页和启动时变更检测，降低独立启动负担。
  - 非 sidecar 的目标 Unreal 项目后续将默认允许可见启动，不再被统一 `windowsHide: true` 强制隐藏；sidecar 仍保持隐藏启动。
  - 避免 `repair` / `status` 因 Zen 子进程卡死而把整个 DCC 环境管理拖住。
- 重启 API 后确认：
  - `http://127.0.0.1:8792/api/health` 正常。
  - `http://127.0.0.1:8792/api/dcc/environment/status?force=1` 可快速返回。
  - Unreal Zen 运行时已恢复到 `127.0.0.1:8558` 可达。

### 2. Blender 已完成一次真实 DCC 节点连通验收

- 真实验收命令：

```powershell
$env:HMDAO_DCC_ENGINE='blender'
$env:HMDAO_DCC_REAL_GENERATION='0'
node server/verify-dcc-browser-flow.mjs --headless
```

- 真实验收产物目录：
  - [summary.json](/abs/path/F:/Work/HMDAODAO/app/artifacts/dcc-browser-headless-2026-07-03T08-36-22-662Z/summary.json)
  - [run.log](/abs/path/F:/Work/HMDAODAO/app/artifacts/dcc-browser-headless-2026-07-03T08-36-22-662Z/run.log)
- 本次已确认通过的关键节点：
  - 画布内成功创建 DCC 节点。
  - 自定义纵向/横向分辨率切换成功。
  - Blender 真实预览成功连入，未停留在 mock。
  - `dcc-capture` 成功派生图片 region 节点。
  - `dcc-record` 成功派生视频 region 节点。
  - 录制结束后预览自动暂停，说明节点状态流转正常。

### 3. 当前 Blender 宿主状态

- 当前这轮新的独立启动证据：
  - `blender.exe pid: 30864`
  - 主窗口：`(未保存) - Blender 5.0.1`
  - `127.0.0.1:8766` 可达。
  - `/api/dcc/status` 中 Blender 为 `reachable: true`。
- Blender 真实 DCC 节点连通验收仍以上面的 `dcc-browser-headless-2026-07-03T08-36-22-662Z` 为准，当前宿主运行态与服务状态已重新对齐。

## Unreal 当前真实状态

### 1. 安全 sidecar 项目

- 启动项目：
  - [HMDaoBuild.uproject](/abs/path/F:/Work/HMDAODAO/app/tmp-unreal-sidecar/HMDaoBuild.uproject)
- 当前证据：
  - 进程可以独立拉起。
  - `/api/dcc/environment/status?force=1` 能识别该项目为运行中的 target project。
  - Zen/DDC 已 ready。
  - 但 `directBridgeOnline` 仍然是 `false`。
- 历史强证据：
  - [app/.hmdao-data/dcc-environment-manager/logs.json](/abs/path/F:/Work/HMDAODAO/app/.hmdao-data/dcc-environment-manager/logs.json) 中，`2026-07-01T05:44:38Z` 附近多次记录 sidecar 卡在：
    - `Turnkey SDK detection complete`
  - 随后被环境管理器判定为 stalled 并自动重启。

### 2. Action 项目

- 启动项目：
  - [Action.uproject](/abs/path/F:/HMDAO_ASCII/UEAction/Action.uproject)
- 当前证据：
  - 进程可以独立拉起。
  - `/api/dcc/environment/status?force=1` 已识别：
    - `hostProcessRunning: true`
    - `targetProjectRunning: true`
    - `project.pid: 13088`（2026-07-03 17:13:13 本地时间这轮可见窗口独立启动）
  - 官方采集前置插件已齐。
  - 当前这轮真实启动日志已连续推进到：
    - `Slate UI initialized`
    - `Shader warmup and target platform discovery`
    - `Turnkey SDK detection complete`
  - `Action.log` 已出现这轮关键真实证据：
    - `LogZenServiceInstance: ... status: OK!`
    - `LogTurnkeySupport: Completed SDK detection: ExitCode = 0`
  - 已触发一次真实的 `custom-plugin-direct` connect：
    - job id: `1783071544673-unreal-connect-wbms42`
    - 当前行为：持续等待独立启动中的 `Action.uproject` 完成后续阶段，不再像旧逻辑那样 2 分钟超时失败
  - 旧逻辑的强反证：
    - `1783071544673-unreal-connect-wbms42` 最终在 `2026-07-03T09:50:58Z` 失败，证明旧等待策略仍会在 `Turnkey` 后过早放弃。
  - 新一轮“干净重启 + 轻量启动配置”证据：
    - `Saved/Config/WindowsEditor/EditorPerProjectUserSettings.ini` 已被自动标准化 5 项启动配置。
    - 新的独立启动进程：
      - `UnrealEditor.exe pid: 13088`
      - `CrashReportClientEditor.exe pid: 27620`
      - 启动时间：`2026-07-03 18:34:37`（本地时间）
    - 当前可见窗口证据：
      - `Action - 虚幻编辑器`
      - `MainWindowHandle: 6229370`
    - 新的本轮专用日志：
      - [Action-hmdao-launch.log](/abs/path/F:/HMDAO_ASCII/UEAction/Saved/Logs/Action-hmdao-launch.log)
    - 本轮新日志已确认推进到：
      - `Project plugin mounted`
      - `Shader warmup and target platform discovery`
      - `LogTurnkeySupport: Completed SDK detection: ExitCode = 0`
      - `LogAssetRegistry: FAssetRegistry took 0.0034 seconds to start up`
      - `LogEditorDomain: Display: EditorDomain is Disabled`
      - `LogHttpListener: Created new HttpListener on 127.0.0.1:30010`
      - `LogRemoteControl: Web Remote Control WebSocket server started on port 30020`
      - `HMDao Unreal Capture: StartupModule`
      - `HMDao Unreal Capture: startup connect flag detected`
      - `HMDao Unreal Capture: Connect -> ws://127.0.0.1:8792/ws/dcc/unreal?role=plugin`
      - `HMDao Unreal Capture: WebSocket connected`
    - `/api/dcc/status` 当前已确认：
      - `engines.unreal.reachable: true`
      - `engines.unreal.integration: custom-plugin-direct`
      - `engines.unreal.directBridgeOnline: true`
      - `engines.unreal.directBridgeReadyForTargetProject: true`
      - `engines.unreal.previewProvider: editor-direct`
      - `engines.unreal.remoteControlUrl: http://127.0.0.1:30010`
    - 本轮真实浏览器 DCC 验收产物：
      - [summary.json](/abs/path/F:/Work/HMDAODAO/app/artifacts/dcc-browser-headless-2026-07-03T10-36-11-090Z/summary.json)
      - [run.log](/abs/path/F:/Work/HMDAODAO/app/artifacts/dcc-browser-headless-2026-07-03T10-36-11-090Z/run.log)
    - 本轮已验证通过的 Unreal 画布/DCC 节点关键点：
      - 成功新建 DCC 节点。
      - 纵向 / 横向分辨率切换通过。
      - `dcc-preview-live` 成功，真实预览来自 `editor-direct`。
      - 已读到真实相机选项；当前隔离启动世界仅返回 1 个可选相机，因此 `camera-switch-diff` 被脚本按 `less-than-two-cameras` 跳过。
      - `dcc-capture` 成功派生图片 region 节点。
      - `dcc-record` 成功派生视频 region 节点。
      - 录制结束后预览自动暂停。

## 当前判断

- Blender 真实链路已经具备“可复验通过”的证据。
- Unreal 现在已经完成“独立可见启动 + Remote Control 可达 + HMDao 直连桥上线 + 画布 DCC 节点真实接入”的整条真实链路。
- 当前剩余的唯一弱项不是桥接或节点连通，而是隔离启动后的空编辑器世界只有 1 个相机选项，因此本轮没有执行 `camera-switch-diff` 的双相机差异验证。
- 就用户当前目标“独立正常运行启动验证 + DCC 节点连接验证”而言，本轮已经具备完成证据。

## 最终一次真实验收的执行顺序

### A. Unreal 先补齐可观测性

1. 先解决 Unreal 当前日志使用的是旧相位/旧时间戳的问题。
2. 必须拿到本次启动的真实日志文件或前台阻塞画面证据。
3. 如果是首启弹窗、Turnkey、工程修复、插件确认框导致停住，优先清掉前台阻塞。

### B. Unreal 真桥接过线标准

以下条件必须同时为真：

1. `/api/dcc/status` 中：
   - `engines.unreal.directBridgeOnline === true`
   - `engines.unreal.directBridgePlugin != null`
2. `/api/dcc/environment/status?force=1` 中：
   - `targetProjectRunning === true`
   - `plugin.cameraCount > 0`
3. Unreal 端至少出现一次本次启动的真实握手日志：
   - `HMDao Unreal Capture: StartupModule`
   - `HMDao Unreal Capture: Connect -> ws://127.0.0.1:8792/ws/dcc/unreal?role=plugin`
   - `HMDao Unreal Capture: WebSocket connected`

### C. 画布最终真实验收

当 Unreal 真桥接过线后，执行：

```powershell
$env:HMDAO_DCC_ENGINE='unreal'
$env:HMDAO_DCC_REAL_GENERATION='0'
node server/verify-dcc-browser-flow.mjs --headless
```

该轮必须至少验证：

1. 画布新增 DCC 节点正常。
2. Unreal 真实预览接入成功。
3. 相机列表能进节点下拉框。
4. 相机切换能引起预览差异。
5. `dcc-capture` 能派生图片 region 节点。
6. `dcc-record` 能派生视频 region 节点。

## 下一轮优先项

1. 处理 Unreal 启动过程里的“旧日志相位 + 新进程未握手”问题。
2. 必要时改为一次可见窗口启动，确认是否有前台阻塞。
3. 只要 `directBridgeOnline` 过线，立刻执行 Unreal 版浏览器 DCC 验收。
