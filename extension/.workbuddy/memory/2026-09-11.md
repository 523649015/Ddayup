# 2026-09-11 工作日志

## 侧栏卡片堆叠遮挡修复（BugFix）
- 问题：Ddayup 扩展侧栏素材卡片过多时相互堆叠遮挡，看不清内容。
- 根因：`#list` 仅 3 列、min-height 仅 200px，卡片被压扁/挤出；非虚拟化路径无绝对定位问题（HMDAO_VIRTUALIZE=false）。
- 修复（sidepanel.css / sidepanel.js / card-render.js）：
  - `#list` 改为 `grid-template-columns: repeat(5, 1fr)` + `align-items: stretch`，`min-height` 200px → 420px（约一倍，稳定显示 5 排）。
  - `hmdaoListMetrics` 中 COLS 兜底 3 → 5（与 CSS 对齐）。
  - build 标记更新为 `2026-09-11-card-layout-fix`。
- 验证：QA node --check 语法通过；grid 布局推导在 320–400px 侧栏下 5 排稳定显示、超出滚动；虚拟化路径未误触发绝对定位。PASS。

## 侧栏卡片布局二次修正（3列大卡片 + 区域加高）
- 反馈：首版误改成 5 列小卡片，用户要求与参考图一致——3 列大卡片，展示区整体加长加大、卡片不变小。
- 修正：
  - `sidepanel.css` #list 改回 `repeat(3, 1fr)`；`min-height` 420px → 700px（3列时每列约112px、每排~116px，700px 可稳定显示5-6排）。
  - `card-render.js` hmdaoListMetrics COLS 兜底 5 → 3（与CSS对齐）。
  - build 标记改为 `2026-09-11-card-layout-3cols-fix`。
- 验证：QA node --check 通过；grid 推导与参考图一致；虚拟化关闭路径无 absolute 堆叠。PASS。

## 排障提醒：改扩展 JS 后必须重载扩展（非刷新侧栏）
- 现象：sidepanel.js 重构时短暂留多余 `}` → 旧文件整段解析失败 → 浏览器报 `Unexpected token '}'` @1628、`setStatus/currentFilter is not defined`、侧栏卡"加载中"。
- 已移除多余 `}`，磁盘文件 node --check 通过 + B站采集测试 20/20 通过。
- 关键：Chrome MV3 改完 JS 后，**只刷新侧栏页面不会重新加载脚本**，必须 `chrome://extensions` 点扩展的 🔄 重载图标，再重开侧栏；否则跑的是旧缓存（旧语法错误）。
- 扩展 ID：fjljkjllklpccpcnnjppoammgjcbggno。无 build 目录/脚本，manifest 直接加载 sidepanel.js。

## 侧栏卡片响应式布局 + 既有语法错误修复
- 反馈：窄侧栏下 3 列被压成细条看不到封面。改为响应式 grid：`repeat(3, minmax(110px,1fr))` + 媒体查询（<360px 降 2 列、<240px 降 1 列），卡片最小 110px 始终可见封面；gap 4→8、padding 8→10、标题 9→10px。build 标记 `2026-09-11-card-responsive-fix`。
- 关键阻断：QA 回归发现 sidepanel.js 存在既有语法错误（doBatchCollect 函数后多余一个 `}`），node --check 直接 FAIL，会导致整段脚本不执行、侧栏空白。已删除该多余 `}`，验证 doBatchCollect(1571-1626) 括号嵌套完整、语法 SYNTAX_OK。
- 经验：修改后务必 node --check 校验；用户截图为旧版/构建产物，工作区源码已有语法错误需修复才能 reload 生效。

## 「按类型自定义保存路径」双保险 + 相对子目录兜底（BugFix + 增强）
- 背景：用户在侧栏「按类型自定义保存路径」选目录后界面仍显示「未设置」，且下载文件未落到自定义路径。根因是 `showDirectoryPicker`（File System Access API）在 sidePanel 上下文常被浏览器静默拒绝/不支持，导致绝对目录写入失效。
- 方案（双保险）：① 绝对目录按钮 `.pathPick`（`showDirectoryPicker`）优先；② 新增相对子目录文本输入 `.pathInput` + 应用按钮 `.pathApply`——写入浏览器下载目录 `Ddayup/<相对子目录>/`（chrome.downloads.download 的相对子路径 100% 可用）。两者皆无则用默认类型子目录。
- 改动文件：
  - `sidepanel.html`：6 个 `.pathRow` 增加 `data-type`、`.pathPick`(带 title 提示)、`.pathInput`(占位/title 说明绝对盘符不可写)、`.pathApply`、`.pathText`/`.pathTest`/`.pathClear`(title 更新)。
  - `sidepanel.js`：新增全局 `var relDirs`（与 `saveHandles` 并列）。
  - `sidepanel.css`：新增 `.pathRow/.pathPick/.pathInput/.pathApply/.pathText(.ok/.warn)/.pathTest/.pathClear` 样式。
  - `bulk-actions.js`：`normalizeRelPath`(剥离 file://、D:\/D:/、前导 /)、`saveRelDir/loadRelDir`、`userRelDirFor`(netdisk 复用 archive)、`restoreRelDirs`、`applyRelPath` + 绑定 `.pathApply`/`.pathInput` 事件；`.pathClear`/`.pathTest` 兼顾相对子目录。
  - `download.js`：`dlViaChrome` 内 tryWriteUserDirFromUrl 失败/未设置后，若 `userRelDirFor` 返回相对路径，改写 `filename` 为 `Ddayup/<rel>/<name>` 兜底。
- 验证：新增 `tests/phase16-userdir-relfallback.test.mjs`（C1 文本框设相对→relDirs+回显；C2 绝对盘符剥离；C3 userRelDirFor；C4 dlViaChrome 改写 filename 且不透传 asset；C5 netdisk 复用 archive；C6 未设置维持默认）。修复测试 chrome stub 缺失 `runtime.onMessage`/`tabs.onActivated` → 改用 wrapFallthrough 子键回落 deepStub、`.pathText` 补 classList。
- 回归：phase1–16 全绿（17/17 exit=0）。phase15（10/0）验证绝对目录路径仍有效。
- 待办：按用户约定「截图分析成功再提交」，未 commit；建议真机 reload 扩展后验证：点「选择目录」成功显示✓/失败显示⚠原因；文本框填入相对子目录→应用，下载确认落到 浏览器下载目录/Ddayup/<输入>/。

## HMDAO_FRAME_BUDGET 分批懒加载改造 + 回归修复（本会话）
- 需求：把抽帧预算从「会话硬上限 12 次」改为「每轮批次 12 张，完成后若仍有缺封面卡自动续批，无卡了自动停止，失败集防死循环」。
- 实现（software-engineer-2，card-render.js +1263 行）：新增 `hmdaoEnqueueFrameExtract`(幂等入队)/`hmdaoFramePump`(分批+续批调度)/`HMDAO_FRAME_BATCH=12`/`HMDAO_FRAME_FAILED`(Set)/`HMDAO_FRAME_QUEUE`/`HMDAO_FRAME_ACTIVE`/`HMDAO_FRAME_CONCURRENCY=2`；`extractFrameCoverNoCover`→`hmdaoRunFrameExtract`；三处触发点收口到 `hmdaoEnqueueFrameExtract`。`HMDAO_FRAME_BUDGET` 语义改为「每轮批次大小」。
- 验证：新增 `tests/phase17-frame-budget-batch.test.mjs`（C1 续批>12、C2 失败集只抽1次、C3 自动停止、C4 并发≤2），4/4 通过。
- **回归修复（lead 直接修）**：工程师重写引入了 2 个回归——
  1. phase14 全 9 项 `Cannot read properties of undefined (reading 'setProperty')`：新增加载期 IIFE `hmdaoWatchListSize` 在 load 时调用 `hmdaoUpdateCardSize()` → `c.style.setProperty`，而 phase14 桩 `list` 元素无 `.style`。修复：`hmdaoUpdateCardSize` 首行加 `if (!c || !c.style) return;` 守卫（真机 DOM 必有 .style，无副作用）。
  2. phase13 K8「脚本加载顺序」失败：工程师给 `sidepanel.html` 的 `card-render.js` 脚本加了 `?v=20260911c` 缓存版本号，导致测试精确串 `src="card-render.js"`（带闭引号）匹配不到。修复：K8 断言改为正则容忍 `?...` 查询串，仍校验基础文件名与顺序。
- 回归：phase1–17 全部 18 套件 0 失败（PASS=18）。
- **未 commit**：工作区尚有大量未提交改动（manifest 0.1.3→0.2.6、i18n、licence badge、水印屏蔽、文档 chip 等，属 0.2.x 既有工作树，非本次范围）。按用户约定真机验证后再提交。

## B站首页卡片无封面/无内容的机制澄清（待用户确认是否做）
- 无封面根因：`scan.js` 仅在单个视频播放页读 `window.__INITIAL_STATE__.videoData.pic` 拿封面；**首页 feed 不走这条**，feed 卡无 `a.cover`。需单独改 `scan.js` 解析 `.bili-video-card` 缩略图+标题才能给首页卡上封面（属新功能，未做）。
- 无内容根因：`a.url` 是视频页链接/bare bilivideo 流，可播放直链需源页正在播放时由 `preview-render.js` 的 `HMDAO_REFRESH_FROM_PAGE` 生成 WBI durl；首页未播放 → 点进去无内容。**首页播放从根本上不可得**（需源页在播）。
- runninghub/即梦/iMini「有内容无封面」≠ 预算坏掉，而是 12 上限 + 视口时机导致的预算饥饿；分批懒加载（本次改造）可缓解。

## 「截图识文」能力架构评审（software-company 专家团队）
- 触发：侧栏智能机器人(#aiBotBtn, initAiBot@sidepanel.js~L5471) 悬停1s→「截图识文」→自定义区域截图(含长截图/跨视口)→翻译/提取文字/复制截图→粘微信。
- 评审落盘：`F:\Work\HMDAODAO\extension\ARCHITECTURE_REVIEW_screenshot-ocr.md`（架构师高见远）。
- 关键结论：
  - **受限页**：chrome:// / chrome-extension:// / edge:// / about:* / view-source: / DevTools / 网上应用店 / data: 必然截不了；file:// 需「允许访问文件网址」；http(s)+localhost 可截。扩展截的是网页内容区非整屏。现有 L1831-1834 try/catch 吞异常→受限页会静默无反应，需友好提示。
  - **长截图**：默认方案1=滚动+逐视口 captureVisibleTab+Canvas 拼接(0新权限)；方案2=chrome.debugger+CDP captureBeyondViewport(需 debugger 权限+持久黄条，可选高质量模式)。
  - **不传画布**：移除 HMDAO_AGENT_UPLOAD；截图仅本地裁切+复制+发后端 OCR/翻译。
  - **架构/臃肿**：sidepanel.js 6580行 God File，技术债重；截图识文严禁塞进主文件，必须独立成 features/ 模块。
  - **可插拔**：FeatureManager + window.HMDAO_FEATURES 自注册 + 后台 HMDAO_BG_HANDLERS 注册表；增删=增删一个文件+sidepanel.html一行script(+background.js一行importScripts)；支持 options 页开关。
  - 文件清单：features/feature-manager.js、screenshot-ocr.js、screenshot-select-inject.js、screenshot-ocr-bg.js；后端需新增多模态图片接口(/api/extension-ocr 或扩展 extension-ai 收 image)——P0 阻塞。

## 「截图识文」已按优先级落地实现（可插拔，未提交）
- 基础设施：新增 `features/feature-manager.js`（FeatureManager + window.HMDAO_FEATURES 自注册 + storage 开关 `hmdao:feature:screenshotOcr`，默认开）。`router.js` 暴露 `globalThis.registerHandler`（新增一行）。
- 后台：`features/screenshot-ocr-bg.js` 经 registerHandler 自注册 SCREENSHOT_CAPTURE(视口png高保真,受限页返 restricted:true)/CAPTURE_LONG(OffscreenCanvas 滚动拼接,藏 fixed 头部)/OCR(发 /api/extension-ocr)；`background.js` 顶部 importScripts 一行接入。
- 注入：`features/screenshot-select-inject.js`（files 注入，含 shared/messages.js 以用 HMDAO_MSG 常量）渲染拖拽矩形选区，回传 {x,y,w,h,dpr,scrollX,scrollY,vw,vh}。
- 侧栏：`features/screenshot-ocr.js` 自注册；#aiBotBtn 悬停1s→「区域截图/截长图」浮层（受限页显示提示）；区域模式注入选区→裁切(自动用 img.width/vw 修正缩放规避dpr)；结果面板 翻译/提取文字/复制截图(ClipboardItem image/png)。
- 接线：sidepanel.html 在 shared/messages.js 后加 feature-manager.js + screenshot-ocr.js 两行；options.html/options.js 加「截图识文」开关（change 即写 storage）。
- 校验：node --check 全绿（含 options.js ESM）；消息键 shared/messages.js 与前后端一致；findHmdaoTab 为 background 全局函数可供 bg 模块复用。
- 已知/待办：① 后端 /api/extension-ocr 尚未实现 → OCR/翻译会返回明确错误提示（P0 需后端接入多模态）；② 长截图 fixed 头部仅在首片保留、其余隐藏（已知取舍）；③ 改完需 chrome://extensions 重载扩展(非刷新侧栏)真机验证；④ 未 commit。
- 复用点：copyImageToClipboard@L413、paintHoverFrame@L4991(canvas裁切)、boxSelectPageText@L6365(executeScript注入选区)、captureVisibleTab@L1831(改 png 高保真)。
